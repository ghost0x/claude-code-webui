# Authentication Configuration
# Set these environment variables to enable basic authentication
# Leave these unset to disable authentication (default behavior)

# Username for the single allowed user
AUTH_USERNAME=admin

# Password for the single allowed user  
AUTH_PASSWORD=your-secure-password

# Optional: Backend port configuration
PORT=8080

# Optional: Enable debug logging
DEBUG=false